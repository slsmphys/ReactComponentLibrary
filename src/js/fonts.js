const fontstack1 = `roboto, arial, helvetica, sans-serif`;

const fontweight = {
    light: 300,
    regular: 400,
    medium: 500,
    bold: 700,
};

export default fontweight;

export { fontstack1 };
