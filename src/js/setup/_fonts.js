const fallbackFonts = `Arial, Helvetica, sans-serif`;

const fonts = {
    fontFamilies: {
        stack1: "'roboto-regular', Arial",
    },
    fontWeights: {
        light: 300,
        regular: 400,
        medium: 500,
        bold: 700,
    },

    primary: `"Roboto"`,
};

export default fonts;
